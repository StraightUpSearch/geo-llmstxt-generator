# Anthropic API key for AI-enhanced llms.txt generation
# Get your key at https://console.anthropic.com
# Optional: without it, the tool generates a structural (non-AI) output
ANTHROPIC_API_KEY=sk-ant-...

# Server port (default: 3000)
PORT=3000
